#include "main.h"
/**
* main - Entry point
*
* print_alphabet_x10
* Return: Always 0
*/

void print_alphabet_x10(void)
{
	int line, ch;

	for (line = 0; line <= 9; line++)
	{
		for (ch = 'a'; ch <= 'z'; ch++)

putchar(ch);
putchar('\n');

	}
	}

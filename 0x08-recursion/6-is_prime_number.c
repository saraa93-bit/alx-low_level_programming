#include "main.h"

/**
* is_prime_number - returns 1 if n is a prime number, else return 0.
*
* Description: a function that returns 1 if n is a prime number,else return 0.
* @n: input number
*
* Return: 1 if the input integer is a prime number, otherwise return 0.
*/

int is_prime_number(int n)
{
	if (n == prime number)
	{
		return (1);
	}
	else
	{
		return (0);
	}


}
